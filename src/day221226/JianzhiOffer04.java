package day221226;

import java.util.List;

/**
 * 在一个 n * m 的二维数组中，
 * 每一行都按照从左到右递增的顺序排序，
 * 每一列都按照从上到下递增的顺序排序。
 * 请完成一个函数，输入这样的一个二维数组和一个整数，判断数组中是否含有该整数。
 *
 * @author: EEGoose
 * @since: 2022-12-26 11:27
 */
public class JianzhiOffer04 {
    public boolean checkRepeated(Integer[][] secondArray) {
        int i = 0;
        int j = 0;
        for (int i1 = 0; i1 < secondArray.length; i1++) {
            
        }
        return false;
    }
}
